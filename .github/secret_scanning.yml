# samples/ holds deliberately credential-shaped values so the extension has
# realistic input to detect and so the demo recording shows real findings.
# Nothing in there is live.
#
# This excludes the directory from secret-scanning alerts. Whether it also
# exempts push protection is verified below rather than assumed.
paths-ignore:
  - "samples/**"
